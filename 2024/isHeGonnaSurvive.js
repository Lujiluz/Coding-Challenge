function hero(bullets, dragons) {
  if (dragons * 2 === bullets || dragons * 2 < bullets) return true;
  return false;
}

// Done By Me🫡

function bestPractice(bullets, dragons) {
  return bullets >= dragons * 2;
}

// Above is the best practice🚀
